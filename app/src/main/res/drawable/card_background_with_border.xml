<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

  <!-- Obramowanie -->
  <stroke
      android:width="6dp"
      android:color="#000000" /> <!-- Czarna ramka -->

  <!-- Zaokrąglone rogi -->
  <corners android:radius="12dp" />

  <!-- Tło -->
  <solid android:color="#fbc31d" />
</shape>
